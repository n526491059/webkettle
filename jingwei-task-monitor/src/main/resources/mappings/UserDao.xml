<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sxdata.jingwei.dao.UserDao">
    <!--根据登录名查找用户-->
    <select id="getUserbyName" resultType="org.sxdata.jingwei.entity.User">
        select
            r.DESCRIPTION as decription,
            r.ID_USER as userId,
            r.LOGIN as login,
            r.`PASSWORD` as password,
            r.`NAME` as name,
            r.ENABLED as enabled
        from r_user r where r.`login` = #{name}
    </select>

</mapper>